%ul
  - @current_user_teams.each do |t|
    %li
      = "#{t.city} #{t.name}"
      %span.delete
        = link_to "Delete", {:controller => "picks", :action => "destroy", :id => current_user.id, :team_id => t.id}, method: :delete